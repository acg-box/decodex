# Visual references

These PNG files are the user-selected concept references. Their hashes are in
`sha256.json`. They are not installed as flattened icons.

The current implementation is in `../liquid-glass`. It uses native SVG geometry
and Icon Composer materials, with Default, Dark, and Mono annotations. The old
static export route was rejected because it did not provide native Liquid Glass.
