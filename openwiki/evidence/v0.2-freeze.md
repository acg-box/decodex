# Decodex v0.2 Freeze Receipt

Status: frozen with one explicit legacy-evidence gap. This is the repository-owned
XY-1261 inventory and cutover prerequisite receipt.

Captured: 2026-07-13T04:45Z. Repository paths in the attached inventories replace the
local home directory with `<home>`; no credential, token, prompt, or account content is
checked in.

## Trusted boundary and tag

The final trusted v0.2 boundary is
`f9d6c4e70198e94e5b9461b8cac7518ae14d41ef`. At freeze time, the provided worktree
`HEAD`, local `main`, and `origin/main` all resolved to this commit. It contains the
landed XY-1260 vNext authority documents on top of planning snapshot
`5546dcb3b2eb0a8aecf7d6a3b117d2605ea315b8`; it contains no vNext runtime
implementation.

Existing product releases use signed annotated `vX.Y.Z` tags plus GitHub releases:
`v0.1.0` and `v0.2.0`. The freeze marker is intentionally not another product release.
Signed annotated tag `legacy-v0.2-final` has tag object
`36708769264bbcbdbb6583e7058d5e93f945a4f0` and peels locally and remotely to the exact
trusted boundary above. `git verify-tag legacy-v0.2-final` reported a good signature.
No GitHub release was created for the freeze marker.

## Runtime and cold backup

No `decodex serve` process or launchd service was running before the freeze. The two
observed `decodex` processes were scoped `git-hook pre-push` processes, not runtime
managers, and were not terminated. A fresh post-operation process scan is required before
cutover.

The machine's expected `~/.codex/decodex/runtime.sqlite3` database file was already
absent before XY-1261 started. The path was a read-only directory containing only a
`RETIRED` sentinel with SHA-256
`528ccaba680f7952526e1169067bf4417bd1bc591b46028689d96f848ee6efa8`.
`decodex status` failed with SQLite error 14, and bounded filesystem, Spotlight, open-file,
and backup-directory scans found no legacy `runtime.sqlite3` file. XY-1261 did not delete,
replace, reconstruct, or import a database.

A mode-0700 cold copy exists outside the repository and outside future former server store state
at:

```text
~/.codex/backups/decodex-v0.2-final-20260713T043803Z
```

Its 51 allowlisted payload files are 25 non-automation files (the remaining global
config, account-pool file, eleven `project.toml`/`WORKFLOW.md` pairs, and retirement
sentinel) plus all 26 live automation definitions. With the sole metadata file
`SHA256SUMS`, the cold-copy root contains 52 files. All directories are mode 0700 and all
files are mode 0600. Symlinks and non-allowlisted files were rejected. The `SHA256SUMS`
file has SHA-256
`10c22f9676f0e00467540f3c297d38f07435e76194362b14af95d04ebf57ee50`.

Validation compared every copied byte with its source, verified every checksum, parsed
all copied TOML, and parsed every copied account JSONL row without starting the runtime.
Codex sessions, rollout history, Codex state databases, logs, goals, memories, and runtime
history were explicitly excluded. The account-pool copy is secret-bearing cold material;
only its metadata belongs in this receipt.

[The redacted config inventory](v0.2-freeze-config.tsv) publishes the exact 25 logical
paths, per-file hashes, sizes, modes, sensitivity classes, and vNext dispositions without
publishing file contents or machine-local absolute paths. Together with
[the automation inventory](v0.2-freeze-automations.tsv), it is the repository-owned
51-file payload inventory; `SHA256SUMS` remains the byte-level cold-copy authority.

The required SQLite restore-read test is **not possible**: no database bytes existed to
copy. The retirement sentinel restored and read successfully, but it is not a SQLite
backup. This is a pre-existing evidence loss and an unresolved XY-1261 acceptance gap,
not a successful database-backup claim.

## Automation freeze

All 26 live Codex App automation definitions were already `PAUSED`; therefore XY-1261
made no automation mutation. In particular:

- `decodex-automation-manager` is the obsolete v0.2 manager path and remains frozen.
- The Decodex Publisher, health, effectiveness, and growth definitions are preserved for
  later explicit review; they are not implicitly adopted into vNext.
- PubFi, Radar, Publisher, Codex archive, and Dexter automations remain unrelated
  inventory. They were not disabled, deleted, or reclassified merely because they use
  Decodex today.

The exact live IDs, display names, states, definition hashes, and dispositions are in
[the automation inventory](v0.2-freeze-automations.tsv). vNext must recreate only
explicitly reviewed Automations after the owning gate.

## Obsolete Lane Authority v2 work

The following external work was frozen without merge, branch deletion, worktree cleanup,
or wholesale reuse:

| Surface | Frozen state |
| --- | --- |
| PR #1092 | Closed draft, never merged; exact head `32a0589b94987f265013ffd3c8b322f9c57f5097`; 292 changed files, +18,351/-727. |
| PR #1090 | Closed draft, never merged; exact head `85454ccf28f0fd3cb9cf43ca4ec5c16e188f2af9`; 67 changed files, +17,050/-8. |
| Linear XY-1251 | Canceled as superseded; freeze comment records both exact heads. |
| PR #1073 / XY-1248 | Still open at `2377fb44a3c89b7ef8aa46714472d972191bd522`; preserved as related incident input and not changed by XY-1261. |

Both closed PRs have a `Frozen by XY-1261` comment, retain their original branches, and
have `mergedAt = null`. The runtime worktree had 16 dirty entries, including one untracked
source file, and the C1I worktree was clean. Dirty state was deliberately preserved.

The landed historical evidence remains available at these exact SHA-256 identities:

| Evidence | SHA-256 |
| --- | --- |
| `openwiki/decisions/lane-authority-v2.md` | `a4166e7212eda2779dcc311994619325f4e58df7d67844a152a023b783631cde` |
| `openwiki/specs/lane-authority-v2.md` | `498150be06c48bf730e8a7819d10882a3d906164e8dcf40fcb19225ed0de099c` |
| `openwiki/specs/lane-authority-v2-effects.md` | `f71df5afb3e7845fbeedfad83a7042182110f07c1ae49b1601bc1edc01b51b90` |
| `openwiki/specs/lane-authority-v2-gates.md` | `473b45c793ebe7c114fdf5765aa92154de5606141030129f2df6226993d55628` |
| `openwiki/evidence/lane-authority-v2-checkpoints.md` | `5266183c81b4a7bce7ccf258e630bf2fe01fb444e37ca8dc0206ced40b8bbc1b` |
| `apps/decodex/src/orchestrator/tests/fixtures/lane_authority_v2/mutation_registry.json` | `d0cbd97dfe32376d8a1d41a905a7b85bb5b4eee5c77b1cd2c13a219902fdfee8` |
| PR #1092 checkpoint ledger at its exact head | `2e807a7fbeeae71c16cbf2dc5893e860da686c24122709987ff825bcf8e4b60e` |
| PR #1092 `pub_1704_superseded.json` fixture | `f587d8e315f880363b58492294f0b19320bc18d9051a4dc3260dddd4fdb787cc` |

These artifacts preserve tamper, crash/retry, rollback, privacy, writeback, supersession,
and PUB-1704/PUB-1705 incident scenarios. They are provenance only. C1-C7 must not be
continued or treated as vNext gates; a behavior may be reused only after a focused vNext
owner and test make it authoritative.

## Worktrees, branches, and cleanup ownership

[The worktree inventory](v0.2-freeze-worktrees.tsv) records all 48 registered worktrees,
their normalized location, exact head, branch owner, dirty-entry count, and preservation
disposition. Thirty-three had local changes. The vNext XY-1261 through XY-1264 worktrees are
active; Lane Authority v2 worktrees are frozen obsolete evidence; XY-1248 is incident
evidence; every other worktree requires its branch/task owner to review it.

No worktree, branch, remote ref, runtime artifact, or user history was deleted. This
receipt authorizes no cleanup. A later cleanup task must refresh Git/filesystem/GitHub
readback and obtain owner authority rather than treating this as a deletion list.

## Cutover prerequisites and stop conditions

Before destructive v0.2 removal or vNext startup, a later task must:

1. Verify `legacy-v0.2-final` locally and remotely, including signature and peeled target.
2. Verify the cold-copy root permissions, exact file set, `SHA256SUMS`, and parse/read
   checks without starting v0.2.
3. Obtain explicit Manager disposition of the missing legacy SQLite backup. The current
   receipt does not satisfy that acceptance item.
4. Re-read that PRs #1092 and #1090 are closed and unmerged, XY-1251 is canceled, and
   their branches/worktrees/evidence still exist.
5. Re-read every automation and process. Stop if any obsolete v0.2 manager/runtime path
   is active; do not mutate unrelated PubFi/Radar/Publisher jobs.
6. Start with empty former server store product state and explicitly recreate selected Projects
   and Automations from reviewed inventory. Import no Codex sessions, SQLite rows, Linear
   lanes, runtime history, or Codex-created tasks.
7. Leave old Linear/SQLite/Goal/operator-transport source removal to the later replacement
   gate. No dual reads, dual writes, compatibility facade, or hidden v0.2 default is
   authorized here.

Stop if the tag or backup changes, a frozen PR/branch/worktree disappears, an automation
becomes active without explicit adoption, credentials appear in repository/former server store
state, or the missing SQLite evidence is silently treated as recovered.

### XY-1423 clean-cutover clarification

This clarification does not change any at-freeze fact, file identity, checksum, or
accepted receipt above. It narrows prerequisite 6 for final vNext cutover.

The installed product never reads the secret-bearing frozen account-pool copy. For one
local cutover, an operator can create private temporary credential files and submit
them through the ordinary vNext account import command. After every destination
account and Reset Card verifies, the operator deletes those temporary files and the
old account pool. There is no bulk migration parser, manifest, receipt, fallback, or
dual authority in the installed product. Quota starts unknown; usage/profile
projection starts empty; no Codex sessions, rollout history, SQLite rows, Linear
lanes, runtime history, Goals, Projects, or Automations are imported.

## Post-freeze old-route cleanup readback

Read back: 2026-07-20.

This section records owner-authorized cleanup after the 2026-07-13 freeze. It does not
change the at-freeze facts above or the historical
[`v0.2-freeze-worktrees.tsv`](v0.2-freeze-worktrees.tsv) snapshot.

For current guidance, this readback supersedes prerequisite 4 and the old-route
branch/worktree disappearance stop condition above. Deleted old-route branches, remote
branch heads, and related worktrees are not future prerequisites or stop conditions.
All other prerequisites and stop conditions remain.

| PR | Current GitHub state and exact head |
| --- | --- |
| #1061 | Closed and unmerged at `3be55f9c4864c730605aed723b00843b693f136a`. |
| #1068 | Closed and unmerged at `cf5914d5534316a2198f4e4a034f6920de7229a6`. |
| #1073 | Closed and unmerged at `2377fb44a3c89b7ef8aa46714472d972191bd522`. |
| #1090 | Closed and unmerged at `85454ccf28f0fd3cb9cf43ca4ec5c16e188f2af9`. |
| #1092 | Closed and unmerged at `32a0589b94987f265013ffd3c8b322f9c57f5097`. |

All five local branches, remote branch heads, and related worktrees are absent. The
#1068 and #1073 artifacts were already absent. This cleanup removed the #1061, #1090,
and #1092 artifacts.

Each corresponding GitHub `refs/pull/<PR>/head` ref still resolves to the exact table
head. GitHub retains each PR history, comments, and reviews.

The cleanup intentionally discarded the #1092 worktree state after independent review:

- 15 modified tracked files, with binary diff SHA-256
  `ea1498c96c6108f2eea07bad55eb2b6d47fd75dcde13c5d24ee542ab97803086`.
- untracked `apps/decodex/src/tracker/linear/client/resolution.rs`, with SHA-256
  `f35608c0eb32a7dac0da92ebb1ad6185f3f3597aa718ec27662124715fd32677`.

The signed tag object `36708769264bbcbdbb6583e7058d5e93f945a4f0` still peels to
`f9d6c4e70198e94e5b9461b8cac7518ae14d41ef` with a good signature. The cold backup
checksum verification passed.

XY-1248, XY-1250, and XY-1251 are `Canceled` as superseded, not completed. XY-1249
remains separate `Backlog` evidence and was not canceled.

No old implementation is carried into vNext. The #1068 implementation is obsolete.
The current vNext roadmap and XY-1303 continue to own CI, Pages, dependency updates,
signing, notarization, and release requirements.

Historical Lane Authority v2 decision, specification, and checkpoint documents remain
provenance only. They must not be deleted or implemented.

### Final residual cleanup

The same 2026-07-20 readback records two final residual candidates.

- XY-1319 was the legacy manual landing candidate. Cleanup removed worktree
  `.worktrees/xy-1319-manual-land-integrity` and local branch
  `xv/xy-1319-manual-land-integrity`. No remote branch or PR existed.
  The candidate base was `2f5e637a2c65ee88c1946df22d5c3649f664f467`.
  The candidate had 34 modified tracked paths and 4 untracked paths.
  The diff had 1,236 insertions and 191 deletions.
  The frozen tree was `889fad957e9b14521671ebe516c8c9420e8e8456`.
  The binary diff SHA-256 was
  `a38f27cba5a45fbab09fe019a45e701faf4e30470ea06211038c1bf6db904279`.
  The full-index SHA-256 was
  `b30bdbcf7149fd324908aa68d1cf9e4e15cb2db84df3bee5939931dd682c5524`.
- XY-1322 was the legacy v0.2 two-phase bootstrap authority candidate. Cleanup removed
  worktree `.worktrees/xy-1322-manual-landing-authority` and local branch
  `xv/xy-1322-manual-landing-authority`. No remote branch or PR existed.
  The candidate base was `2f5e637a2c65ee88c1946df22d5c3649f664f467`.
  The candidate had 6 modified OpenWiki paths.
  The diff had 520 insertions and 2 deletions.
  The binary diff SHA-256 was
  `429ecd1dcf718fe9c5de46180b911bbcbeaa888b437093db044f9c44ba266680`.
  The full-index SHA-256 was
  `3100c413c5232c12caafcb4a7e7753dd6d99691c84b0abecd0b4dd75cb5594d8`.

An independent skeptic returned `APPROVED_FOR_COMPLETE_CLEANUP`. No implementation or
unlanded documentation bytes require transplantation. Preserve only incident lessons
and provenance.

Linear XY-1319, XY-1322, and XY-1323 are `Canceled` as obsolete, not completed. XY-1324
remains `Duplicate`. Retained provenance includes the Linear histories, PR #1112
identity, hashes, rejection findings, and archived task IDs.

XY-1284 and XY-1285 retain generalized vNext repository and landing ownership. XY-1285
records the policy-configured fast landing requirement. Do not import the legacy shim
design.
