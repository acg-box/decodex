{
  "caveats": [
    "This does not change TUI behavior at runtime for ordinary users; it only prevents a Windows-side compile warning/error path.",
    "The broader Windows and Bazel compatibility work remains in flight, so this should be read as one cleanup step, not the whole story."
  ],
  "confidence": "confirmed",
  "config_flags": [],
  "impact": "low",
  "kind": "behavior_change",
  "lane": "github",
  "proof_points": [
    "The PR body says the unused-variable issue slipped in during PR #16578 and explicitly frames this as part of ongoing Windows bring-up work.",
    "The bundle contains a single-file diff in `codex-rs/tui/src/tui.rs` where `let area = terminal.viewport_area;` is moved under `#[cfg(unix)]`, which directly matches the stated fix.",
    "No runtime logic, command paths, or user-visible rendering branches changed outside that binding placement, so the evidence supports calling this a narrow compile-path correction rather than a new TUI feature."
  ],
  "published_at": "2026-04-03T00:05:45Z",
  "schema": "signal_entry/v1",
  "slug": "windows-tui-builds-stop-tripping-cfg-mismatched-local-binding",
  "source_refs": {
    "commit_urls": [
      "https://github.com/openai/codex/commit/14da169c4bbfc7408837edbabf9b419701ec3516"
    ],
    "items": [
      {
        "kind": "pull_request",
        "meta": "#16633",
        "title": "fix: address unused variable on windows",
        "url": "https://github.com/openai/codex/pull/16633"
      },
      {
        "kind": "commit",
        "meta": "14da169",
        "title": "fix: address unused variable on windows",
        "url": "https://github.com/openai/codex/commit/14da169c4bbfc7408837edbabf9b419701ec3516"
      }
    ],
    "pr_url": "https://github.com/openai/codex/pull/16633",
    "repo": "openai/codex"
  },
  "summary": "Merged PR #16633 moves a `terminal.viewport_area` binding inside the Unix-only branch in `tui.rs`, so Windows builds no longer see an unused local introduced by the earlier zellij redraw work.",
  "title": "Windows TUI builds stop tripping a cfg-mismatched local binding",
  "watch_state": "Watch for more `cfg(unix)` and Windows-specific TUI follow-ups near the redraw and viewport code paths as the cross-platform polish continues.",
  "why_it_matters": "This is a small Windows-only build hygiene fix, but it removes avoidable compiler noise from the TUI path while broader Bazel-on-Windows work is still being stabilized."
}
