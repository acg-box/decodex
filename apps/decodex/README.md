# Decodex Runtime

This package builds the `decodex` CLI and runtime.

The repository-level README and docs remain the canonical entry points for workspace
layout, operator workflow, and the static signal site.
