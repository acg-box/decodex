# Scripts Root

This directory contains executable repository helpers.

- `scripts/assets/` owns asset-generation helpers for checked-in app and tray icon
  assets.
- `scripts/macos/` owns macOS-only app packaging and local bundle verification
  helpers.
